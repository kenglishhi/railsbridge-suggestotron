%h1 Listing of Topics
- @topics.each do |topic|
  .topic
    - if current_user && !current_user_has_voted_on_topic(topic)
      %span.right-corner= link_to "+1", topic_votes_path(topic,:format => 'json'), :class => 'vote-for'
    %h4= topic.title
    %p= topic.description
    %p
      %span.number-of-votes= topic.votes.count
      votes
    - if current_user
      = render 'manage_options', :topic => topic

- if current_user
  #new-topic
    =link_to 'New Topic', new_topic_path
%br
%br

%a#do-coffee{href: '#'} Do Coffee
